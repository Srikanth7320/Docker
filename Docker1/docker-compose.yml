version: '3'

services:
  app:
    build: .
    ports:
      - "7000:7000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
